You can no more use component you need to use element to get specific page....

match and params are not available so you need to use useParam.....

<div className="row center">
            {data.products.map((product) => (
                <Product key={product._id} product={product}></Product>
            ))}
            </div>

            needed to add type: module in package.json ...this allow us to use latest fetaures of es6 modules.....

axios is the library to perform sjsx request to the server...
install eslint to check errors syntax errors in your codes ...
we need to use redux to manage the state of our applicdation...

******** REDO ALL THE THINGS IN #37...... Adding redux in Product Screen********
